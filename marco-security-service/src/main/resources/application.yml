spring:
  application:
    name: marco-security-service
server:
  servlet:
    context-path: /security
    
# I am telling at spring boot where is the eureka server
eureka:
  client:
    serviceUrl:
      #try the value from the system props, or get the fall back
      defaultZone: ${MARCO_EUREKA_SERVER:http://localhost:8090/eureka}